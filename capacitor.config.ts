import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'angular.android.barcode',
  appName: 'angular-android-barcode',
  webDir: 'dist/angular-android-barcode/browser/'
};

export default config;
